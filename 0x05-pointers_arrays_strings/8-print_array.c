#include "holberton.h"
#include <stdio.h>
/**
 * print_array - Print n elements of an array of int.
 * @a: Type int pointer
 * @n: Type int
 * Return: Always 0.
 */
void print_array(int *a, int n);
{
	int n;
	char num;

	for (a = 0; str[n] != 0; n++)
	{
	}
	for (n /= 2; str[n] != 0; n++)
	{
		a = str[n];
		_putchar(a);
	}
	_putchar('\n');
}
