#include "holberton.h"
/**
 * add - Function that adds two integers and returns the result.
 * @a: type number
 * @b: typee number
 * Return: result of the addiion of two integers
 */
int add(int a, int b)
{
	return (a + b);
}
