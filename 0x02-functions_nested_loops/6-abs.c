#include "holberton.h"
#include <stdio.h>
/**
 * Main: Compute the absolute value of an integer
 * Return: 0
 */

int print_last_digit(int n)

{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
